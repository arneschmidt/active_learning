model:
  epochs: 10
  head:
    type: deterministic # deterministic, bnn

data:
  supervision: supervised # 'supervised', 'active_learning'ç
  dir: /data/BasesDeDatos/Panda/Panda_patches_256/
  data_split_dir: /data/BasesDeDatos/Panda/Panda_patches_256/

logging:
  save_images: False
  log_artifacts: True
  interval: 1